import 'package:vocabulary/script/data/vocabulary/vocabulary.dart';

mixin VocabularyEncoder {
  static Future<int> encodeVocToBytes(Vocabulary voc) async {
    // final usage = voc.usage;
    // final info = voc.info;
    // final cust = voc.cust;

    return 1;
    // return
  }
}
